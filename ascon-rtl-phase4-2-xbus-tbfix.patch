diff -ruN a/rtl/ascon_block_unpacker32.v b/rtl/ascon_block_unpacker32.v
--- a/rtl/ascon_block_unpacker32.v	2026-05-09 10:54:10.827425910 +0000
+++ b/rtl/ascon_block_unpacker32.v	2026-05-09 10:54:10.850878142 +0000
@@ -60,8 +60,14 @@
     begin
       if (bytes == 5'd0) begin
         words_for_bytes = 3'd0;
+      end else if (bytes <= 5'd4) begin
+        words_for_bytes = 3'd1;
+      end else if (bytes <= 5'd8) begin
+        words_for_bytes = 3'd2;
+      end else if (bytes <= 5'd12) begin
+        words_for_bytes = 3'd3;
       end else begin
-        words_for_bytes = {1'b0, bytes[4:2]} + ((bytes[1:0] != 2'd0) ? 3'd1 : 3'd0);
+        words_for_bytes = 3'd4;
       end
     end
   endfunction
@@ -80,9 +86,9 @@
   endfunction
 
   function [2:0] last_word_bytes;
-    input [4:0] bytes;
+    input [1:0] byte_rem;
     begin
-      case (bytes[1:0])
+      case (byte_rem)
         2'd0: last_word_bytes = 3'd4;
         2'd1: last_word_bytes = 3'd1;
         2'd2: last_word_bytes = 3'd2;
@@ -92,7 +98,7 @@
   endfunction
 
   assign word_data_o  = select_word(block_q, word_index_q);
-  assign word_bytes_o = word_last_o ? last_word_bytes(block_bytes_q) : 3'd4;
+  assign word_bytes_o = word_last_o ? last_word_bytes(block_bytes_q[1:0]) : 3'd4;
 
   always @(posedge clk or negedge rst_n) begin
     if (!rst_n) begin
diff -ruN a/sim/tb/tb_ascon_aead128_xbus.v b/sim/tb/tb_ascon_aead128_xbus.v
--- a/sim/tb/tb_ascon_aead128_xbus.v	2026-05-09 10:54:10.839288500 +0000
+++ b/sim/tb/tb_ascon_aead128_xbus.v	2026-05-09 10:54:10.860468010 +0000
@@ -182,11 +182,12 @@
     input [127:0] block;
     input integer idx;
     begin
+      // Byte-stream order for internal Ascon blocks {first8, second8}.
       case (idx)
-        0: select_word = block[31:0];
-        1: select_word = block[63:32];
-        2: select_word = block[95:64];
-        default: select_word = block[127:96];
+        0: select_word = block[95:64];
+        1: select_word = block[127:96];
+        2: select_word = block[31:0];
+        default: select_word = block[63:32];
       endcase
     end
   endfunction
